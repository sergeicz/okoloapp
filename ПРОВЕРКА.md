# ✅ Как проверить что всё работает

## 🔍 Ваши данные установлены:

- **Username:** `ebashordie` ✅
- **Telegram ID:** `1093761679` ✅  
- **Статус:** Администратор ✅

---

## 🌐 Откройте приложение:

### Вариант 1: Прямая ссылка
```
http://localhost:8000
```

### Вариант 2: С index.html
```
http://localhost:8000/index.html
```

---

## 👀 Что вы должны увидеть:

### 1. **Партнерские ссылки** (сверху):
- Google (Поисковики)
- Amazon (Магазины)
- YouTube (Видео)
- AliExpress (Магазины)
- Udemy (Образование)

### 2. **Кнопка "Админка"** (внизу страницы):
Если вы не видите кнопку "Админка":
- Нажмите `Ctrl+Shift+R` (жесткое обновление)
- Или `F5` несколько раз
- Откройте DevTools (F12) → Console → проверьте ошибки

### 3. **При нажатии на "Админка":**
Должно открыться:
- **Статистика пользователей:**
  - Всего: 2
  - Подписаны: 2
  - Отписаны: 0

- **Таблица пользователей:**
  - testuser (123456789) ✅
  - ebashordie (1093761679) ✅

- **Форма рассылки:**
  - Поле "Заголовок"
  - Поле "Сообщение"
  - Поле "Ссылка"
  - Кнопка "Отправить всем подписанным"

---

## 🐛 Если кнопка "Админка" не появляется:

### Шаг 1: Проверьте консоль браузера
1. Нажмите `F12`
2. Вкладка **Console**
3. Ищите ошибки (красным)
4. Ищите лог: `Admin check for @...`

### Шаг 2: Проверьте Network
1. В DevTools → вкладка **Network**
2. Обновите страницу (`F5`)
3. Найдите запрос `/api/me`
4. Нажмите на него → вкладка **Response**
5. Должно быть: `{"is_admin":true}`

### Шаг 3: Проверьте логи Worker
Откройте терминал где запущен `npm run dev`:
```
🔐 Проверка админа @ebashordie: ✅
   Список админов: ['testuser', 'ebashordie']
```

### Шаг 4: Проверьте mock-data.js
Откройте файл `worker/mock-data.js` и убедитесь:
```javascript
admins: [
  'testuser',
  'ebashordie',  // ← должно быть БЕЗ @
],
```

---

## 🧪 Тест API вручную:

Откройте **новую вкладку** в браузере и вставьте:

```
http://127.0.0.1:8787/api/health
```

Должно вернуть JSON с `"status":"ok"`

---

## 💡 Быстрая проверка:

### В консоли браузера (F12):

Вставьте и выполните:

```javascript
fetch('http://127.0.0.1:8787/api/me', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({username: 'ebashordie'})
})
.then(r => r.json())
.then(data => console.log('Результат:', data))
```

Должно вывести:
```
Результат: {is_admin: true}
```

---

## 🎯 Проблема: Telegram WebApp API

**⚠️ Важно:** Вне Telegram некоторые данные недоступны!

Frontend пытается получить `user` из:
```javascript
const user = Telegram.WebApp.initDataUnsafe.user;
```

Но вне Telegram это будет `undefined`!

### Решение для локального теста:

Откройте `frontend/index.html` и **временно** добавьте после строки 60:

```javascript
const tg = Telegram.WebApp;
const user = tg.initDataUnsafe.user;

// ВРЕМЕННО для локального теста:
const testUser = user || {
  id: 1093761679,
  username: 'ebashordie',
  first_name: 'Test'
};
// И замените все использования user на testUser
```

Или проще - смотрите что у вас в консоли браузера!

---

## ✅ Итоговый чеклист:

- [ ] Worker запущен (`npm run dev`)
- [ ] Frontend запущен (`python -m http.server 8000`)
- [ ] Открыт браузер на `http://localhost:8000`
- [ ] Видны партнерские ссылки
- [ ] В DevTools (F12) нет ошибок
- [ ] Запрос `/api/me` возвращает `is_admin: true`
- [ ] Кнопка "Админка" видна
- [ ] При клике открывается админ-панель

---

## 🚀 Если всё работает:

Попробуйте:
1. Добавить свою ссылку в `worker/mock-data.js`
2. Сохранить файл
3. Обновить браузер
4. Увидеть свою ссылку!

---

**📞 Нужна помощь?**

Скажите что именно не работает и я помогу!
