# 🚀 Пошаговая инструкция по развертыванию проекта

## 📋 Что понадобится

Перед началом подготовьте:
- ✅ Gmail аккаунт (для Google Sheets)
- ✅ GitHub аккаунт (для фронтенда)
- ✅ Cloudflare аккаунт (для API) - регистрация бесплатная
- ✅ Replit аккаунт (для бота) - регистрация бесплатная
- ✅ Telegram бот (создадим через @BotFather)

---

## ⏱️ Шаг 1: Создаем Telegram бота (5 минут)

### 1.1 Создание бота
1. Откройте Telegram
2. Найдите бота **@BotFather** (официальный бот от Telegram)
3. Отправьте команду: `/newbot`
4. Введите **имя** вашего бота (например: "Мой Сервис Ссылок")
5. Введите **username** бота (должен заканчиваться на `bot`, например: `mylinks_bot`)

### 1.2 Получите токен
После создания бота вы получите сообщение с токеном:
```
Done! Congratulations on your new bot...
Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**⚠️ ВАЖНО:** Сохраните этот токен в надежном месте! Он понадобится позже.

### 1.3 Настройте описание (опционально)
```
/setdescription - добавьте описание бота
/setabouttext - добавьте информацию "О боте"
/setuserpic - загрузите аватар бота
```

✅ **Готово!** Бот создан, токен сохранен.

---

## ⏱️ Шаг 2: Настраиваем Google Sheets (10 минут)

### 2.1 Создаем таблицу

1. Откройте [Google Sheets](https://sheets.google.com)
2. Нажмите **"Создать"** → **"Пустая таблица"**
3. Назовите таблицу: **"Telegram Mini App Data"**

### 2.2 Создаем листы

Создайте **4 листа** с точными названиями (на английском!):

**Лист 1: `users`** (пользователи)
```
В первой строке напишите заголовки:
telegram_id | username | first_name | date_added | subscribed
```

**Лист 2: `partners`** (партнерские ссылки)
```
В первой строке:
title | url | category
```

Добавьте тестовые данные во вторую строку:
```
Google | https://google.com | Поисковики
```

И третью строку:
```
Amazon | https://amazon.com | Магазины
```

**Лист 3: `admins`** (администраторы)
```
В первой строке:
username
```

Во вторую строку добавьте ваш Telegram username **БЕЗ символа @**:
```
ваш_username
```

**Лист 4: `clicks`** (статистика кликов)
```
В первой строке:
telegram_id | url | timestamp
```

### 2.3 Сохраните ID таблицы

Посмотрите на адресную строку браузера. URL выглядит так:
```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz123456789/edit
```

ID таблицы - это часть между `/d/` и `/edit`:
```
1AbCdEfGhIjKlMnOpQrStUvWxYz123456789
```

**⚠️ Сохраните этот ID!** Он понадобится позже.

### 2.4 Создаем Service Account (Google API)

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Нажмите **"Select a project"** → **"New Project"**
3. Название проекта: `telegram-miniapp`
4. Нажмите **"Create"**

### 2.5 Включаем Google Sheets API

1. В меню слева выберите **"APIs & Services"** → **"Library"**
2. В поиске найдите: **"Google Sheets API"**
3. Нажмите на результат
4. Нажмите **"Enable"** (Включить)

### 2.6 Создаем учетные данные

1. В меню слева: **"APIs & Services"** → **"Credentials"**
2. Нажмите **"+ Create Credentials"** → **"Service Account"**
3. Заполните форму:
   - **Service account name:** `telegram-bot`
   - **Service account ID:** (заполнится автоматически)
4. Нажмите **"Create and Continue"**
5. **Grant access** - пропустите, нажмите **"Continue"**
6. **Grant users access** - пропустите, нажмите **"Done"**

### 2.7 Скачиваем JSON ключ

1. На странице Credentials найдите созданный Service Account
2. Нажмите на него (на email вида `telegram-bot@...`)
3. Перейдите во вкладку **"Keys"**
4. Нажмите **"Add Key"** → **"Create new key"**
5. Выберите тип **"JSON"**
6. Нажмите **"Create"**
7. Файл `credentials.json` автоматически скачается

**⚠️ ВАЖНО:** Сохраните этот файл! НЕ ПУБЛИКУЙТЕ ЕГО нигде!

### 2.8 Даем доступ к таблице

1. **Откройте скачанный файл** `credentials.json` в блокноте
2. Найдите строку `"client_email"`:
   ```json
   "client_email": "telegram-bot@your-project.iam.gserviceaccount.com"
   ```
3. **Скопируйте** этот email полностью
4. Вернитесь к вашей **Google таблице**
5. Нажмите кнопку **"Поделиться"** (Share) в правом верхнем углу
6. **Вставьте** скопированный email
7. Убедитесь, что выбраны права **"Редактор"** (Editor)
8. **Снимите галочку** "Уведомить пользователей"
9. Нажмите **"Отправить"** (Share)

✅ **Готово!** Google Sheets настроен.

**Что сохранили:**
- ✅ ID таблицы
- ✅ Файл credentials.json

---

## ⏱️ Шаг 3: Деплоим Backend API (Cloudflare Workers) (10 минут)

### 3.1 Регистрация в Cloudflare

1. Откройте [cloudflare.com](https://www.cloudflare.com)
2. Нажмите **"Sign Up"** (бесплатная регистрация)
3. Подтвердите email

### 3.2 Установка Node.js (если еще не установлен)

1. Скачайте [Node.js](https://nodejs.org/) (выберите LTS версию)
2. Установите с настройками по умолчанию
3. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

### 3.3 Подготовка проекта

Откройте терминал (PowerShell или Command Prompt) в папке проекта:

```bash
# Перейдите в папку worker
cd worker

# Установите зависимости
npm install
```

### 3.4 Авторизация в Cloudflare

```bash
npx wrangler login
```

Откроется браузер. Нажмите **"Allow"** (Разрешить).

### 3.5 Подготовка credentials.json

Нужно превратить файл JSON в одну строку:

**Вариант 1 - Онлайн (проще):**
1. Откройте [jsonformatter.org/json-minify](https://jsonformatter.org/json-minify)
2. Скопируйте **всё содержимое** файла `credentials.json`
3. Вставьте в поле на сайте
4. Нажмите **"Minify JSON"**
5. Скопируйте результат (это будет одна длинная строка)

**Вариант 2 - Вручную:**
Откройте `credentials.json` и удалите все переносы строк, сделав из него одну строку.

### 3.6 Настройка секретов

Выполните команды по очереди:

**Секрет 1 - Токен бота:**
```bash
npx wrangler secret put BOT_TOKEN
```
Вставьте токен вашего бота (из Шага 1.2) и нажмите **Enter**.

**Секрет 2 - ID таблицы:**
```bash
npx wrangler secret put SHEET_ID
```
Вставьте ID вашей Google таблицы (из Шага 2.3) и нажмите **Enter**.

**Секрет 3 - Credentials:**
```bash
npx wrangler secret put CREDENTIALS_JSON
```
Вставьте **минифицированный** JSON (из Шага 3.5, одна строка) и нажмите **Enter**.

### 3.7 Деплой!

```bash
npm run deploy
```

Подождите 10-20 секунд. Вы увидите:
```
✨ Published telegram-miniapp-api (2.34 sec)
   https://telegram-miniapp-api.xxxxx.workers.dev
```

**⚠️ СОХРАНИТЕ ЭТОТ URL!** Он понадобится для фронтенда.

### 3.8 Проверка работы

Откройте в браузере:
```
https://ваш-worker-url.workers.dev/api/health
```

Должны увидеть:
```json
{"status":"ok","timestamp":"2024-02-04T...","version":"1.0.0"}
```

✅ **Готово!** Backend API работает.

---

## ⏱️ Шаг 4: Деплоим Frontend (GitHub Pages) (10 минут)

### 4.1 Создаем GitHub репозиторий

1. Откройте [github.com](https://github.com)
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите **"+"** → **"New repository"**
4. Заполните:
   - **Repository name:** `telegram-miniapp`
   - **Public** (публичный)
   - НЕ добавляйте README, .gitignore, license
5. Нажмите **"Create repository"**

### 4.2 Обновляем конфигурацию фронтенда

Откройте файл `frontend/index.html` в редакторе.

Найдите строку (примерно строка 61):
```javascript
const CONFIG = {
  API_URL: 'https://your-worker.your-domain.workers.dev',
};
```

Замените на ваш URL из Шага 3.7:
```javascript
const CONFIG = {
  API_URL: 'https://telegram-miniapp-api.xxxxx.workers.dev',
};
```

**Сохраните файл!**

### 4.3 Загружаем на GitHub

Откройте терминал в корне проекта:

```bash
# Инициализируем git (если еще не сделано)
git init

# Добавляем все файлы
git add .

# Создаем коммит
git commit -m "Initial commit: Telegram Mini App"

# Добавляем удаленный репозиторий (замените YOUR_USERNAME и telegram-miniapp)
git remote add origin https://github.com/YOUR_USERNAME/telegram-miniapp.git

# Переименовываем ветку в main
git branch -M main

# Загружаем на GitHub
git push -u origin main
```

При первой загрузке GitHub попросит авторизацию - войдите.

### 4.4 Включаем GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **"Settings"** (Настройки)
3. В меню слева выберите **"Pages"**
4. В разделе **"Build and deployment"**:
   - **Source:** Deploy from a branch
   - **Branch:** main
   - **Folder:** / (root)
5. Нажмите **"Save"**

Подождите 1-2 минуты. Обновите страницу.

Вы увидите URL вашего приложения:
```
Your site is live at https://YOUR_USERNAME.github.io/telegram-miniapp/
```

### 4.5 Определяем полный URL

Ваше приложение будет доступно по адресу:
```
https://YOUR_USERNAME.github.io/telegram-miniapp/frontend/
```

**⚠️ СОХРАНИТЕ ЭТОТ URL!** Он нужен для настройки бота.

### 4.6 Проверка

Откройте URL в браузере. Должен отобразиться интерфейс с ссылками Google и Amazon.

✅ **Готово!** Frontend опубликован.

---

## ⏱️ Шаг 5: Деплоим бота (Replit) (10 минут)

### 5.1 Регистрация в Replit

1. Откройте [replit.com](https://replit.com)
2. Нажмите **"Sign Up"** (можно войти через GitHub)
3. Подтвердите email

### 5.2 Создание Repl

1. Нажмите **"+ Create Repl"**
2. Выберите Template: **"Python"**
3. Title: `telegram-miniapp-bot`
4. Нажмите **"+ Create Repl"**

### 5.3 Загрузка файлов

В левой панели (Files):

1. Удалите файл `main.py` (если есть)
2. Нажмите три точки рядом с Files → **"Upload file"**
3. Загрузите из папки `bot/`:
   - `bot.py`
   - `requirements.txt`

Или создайте файлы вручную и скопируйте содержимое.

### 5.4 Настройка Secrets (секретов)

1. В левой панели найдите значок **🔒 "Secrets"** (или Tools → Secrets)
2. Добавьте секреты (нажимайте **"+ New secret"**):

**Secret 1:**
```
Key: BOT_TOKEN
Value: (вставьте токен вашего бота из Шага 1.2)
```

**Secret 2:**
```
Key: SHEET_ID
Value: (вставьте ID таблицы из Шага 2.3)
```

**Secret 3:**
```
Key: WEBAPP_URL
Value: https://YOUR_USERNAME.github.io/telegram-miniapp/frontend/
(URL из Шага 4.5)
```

**Secret 4:**
```
Key: CREDENTIALS_JSON
Value: (скопируйте ВСЁ содержимое файла credentials.json)
```

**Важно:** Для CREDENTIALS_JSON вставьте весь JSON целиком (можно с переносами строк).

### 5.5 Установка зависимостей

В нижней части Replit есть **Shell** (консоль). Выполните:

```bash
pip install -r requirements.txt
```

Подождите 1-2 минуты, пока установятся библиотеки.

### 5.6 Запуск бота

Нажмите большую зеленую кнопку **"Run"** ▶️ вверху.

В консоли должно появиться:
```
INFO - Бот успешно инициализирован
INFO - Бот запущен и готов к работе
```

✅ **Бот работает!**

### 5.7 Сохраните URL Replit

Вверху вы увидите URL вашего Repl:
```
https://telegram-miniapp-bot.YOUR_USERNAME.repl.co
```

**Сохраните этот URL** для следующего шага.

### 5.8 Держим бота онлайн 24/7 (UptimeRobot)

Бесплатный Replit засыпает через время. Используем UptimeRobot для пинга:

1. Откройте [uptimerobot.com](https://uptimerobot.com)
2. Нажмите **"Sign Up"** (бесплатно)
3. После регистрации нажмите **"+ Add New Monitor"**
4. Заполните:
   - **Monitor Type:** HTTP(s)
   - **Friendly Name:** Telegram Bot
   - **URL:** (вставьте URL вашего Repl из предыдущего пункта)
   - **Monitoring Interval:** 5 minutes
5. Нажмите **"Create Monitor"**

✅ **Готово!** Теперь бот будет онлайн 24/7.

---

## ⏱️ Шаг 6: Настраиваем Mini App в Telegram (5 минут)

### 6.1 Настройка кнопки меню

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте команду: `/mybots`
4. Выберите вашего бота из списка
5. Нажмите **"Bot Settings"**
6. Нажмите **"Menu Button"**
7. Нажмите **"Configure menu button"**
8. Отправьте **URL** вашего фронтенда:
   ```
   https://YOUR_USERNAME.github.io/telegram-miniapp/frontend/
   ```
9. Отправьте **текст кнопки**:
   ```
   🚀 Открыть приложение
   ```

### 6.2 Первый тест!

1. Найдите вашего бота в Telegram (по username)
2. Нажмите **"Start"** или отправьте `/start`
3. Бот должен ответить приветственным сообщением с кнопкой
4. Нажмите кнопку **"🚀 Открыть приложение"**
5. Должно открыться ваше приложение с ссылками!

✅ **Поздравляем! Приложение работает!**

---

## ✅ Финальная проверка

Проверьте все компоненты:

### Проверка Frontend
- [ ] Открывается URL GitHub Pages
- [ ] Отображаются ссылки (Google, Amazon)
- [ ] Клики по ссылкам открывают страницы

### Проверка Backend
- [ ] URL `/api/health` возвращает `{"status":"ok"}`
- [ ] Нет ошибок CORS в консоли браузера (F12)

### Проверка Bot
- [ ] Бот отвечает на `/start`
- [ ] Кнопка открывает Mini App
- [ ] В консоли Replit нет ошибок

### Проверка интеграции
- [ ] В Google Sheets → лист `users` появился ваш Telegram ID
- [ ] Видна кнопка **"Админка"** (так как вы в листе admins)

### Проверка админки
- [ ] Нажмите кнопку "Админка"
- [ ] Видна статистика пользователей
- [ ] Видна таблица с вашим username
- [ ] Форма рассылки отображается

---

## 🎉 Готово! Что дальше?

### Добавьте свои ссылки
1. Откройте Google Sheets
2. В лист `partners` добавьте строки:
   ```
   Название | URL | Категория
   ```

### Добавьте администраторов
В лист `admins` добавьте usernames (без @) других админов.

### Протестируйте рассылку
1. Откройте приложение
2. Нажмите "Админка"
3. Заполните форму рассылки
4. Отправьте себе тестовое сообщение

---

## 🔧 Как обновлять проект

### Обновление Frontend
```bash
# Измените файлы
git add .
git commit -m "Описание изменений"
git push

# Подождите 1-2 минуты, GitHub Pages обновится
```

### Обновление Worker
```bash
cd worker
# Измените index.js
npm run deploy
```

### Обновление Bot
1. Остановите бота (Stop) в Replit
2. Измените код
3. Запустите (Run)

---

## 🐛 Решение проблем

### Бот не отвечает
1. Откройте URL Replit в браузере (разбудит бот)
2. Проверьте консоль в Replit на ошибки
3. Убедитесь, что все Secrets добавлены правильно

### Frontend не загружается
1. Проверьте что GitHub Pages включен (Settings → Pages)
2. Подождите 2-3 минуты после push
3. Очистите кэш браузера (Ctrl+Shift+R)

### API ошибки
1. Откройте DevTools (F12) → Console
2. Проверьте что `API_URL` правильный в `index.html`
3. Проверьте `/api/health` в браузере

### Админка не видна
1. Проверьте что ваш username (без @) добавлен в лист `admins`
2. Регистр должен совпадать
3. Перезагрузите приложение

### Google Sheets ошибки
1. Проверьте что Service Account email добавлен в редакторы таблицы
2. Проверьте что листы называются точно: `users`, `partners`, `admins`, `clicks`
3. Проверьте что Google Sheets API включен в Google Cloud Console

---

## 💰 Стоимость

Всё **БЕСПЛАТНО**:
- ✅ GitHub Pages - бесплатно навсегда
- ✅ Cloudflare Workers - 100,000 запросов/день бесплатно
- ✅ Replit - бесплатный план
- ✅ UptimeRobot - бесплатно 50 мониторов
- ✅ Google Sheets - бесплатно
- ✅ Telegram Bot API - бесплатно

**Итого: 0 ₽/месяц**

---

## 📞 Поддержка

Если что-то не получилось:
1. Проверьте каждый шаг еще раз
2. Посмотрите логи в Replit Console
3. Откройте DevTools (F12) в браузере и проверьте Console
4. Перечитайте инструкцию внимательно

---

## ✨ Успехов с вашим проектом!

Теперь у вас есть полноценное Telegram Mini App с админ-панелью и рассылками! 🎉

**Не забудьте:**
- Добавить свои партнерские ссылки в Google Sheets
- Настроить дизайн под себя
- Протестировать все функции
- Добавить других администраторов

**Удачи! 🚀**
