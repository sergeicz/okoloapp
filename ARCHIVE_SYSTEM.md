# 📦 Система архивации неактивных пользователей

## 🎯 Назначение

При обнаружении неактивных пользователей (заблокировавших бота, удаливших аккаунт и т.д.), система **автоматически переносит** их данные в специальный лист `pidarasy` перед удалением из основной таблицы `users`.

---

## 📊 Структура листа `pidarasy`

### Колонки:

| Колонка | Описание | Пример |
|---------|----------|--------|
| **username** | Username пользователя в Telegram | `@sergeicz` |
| **tg_id** | Telegram ID | `123456789` |
| **date on** | Дата подписки на бота | `2026-01-15` |
| **date off** | Дата отписки/блокировки | `2026-02-04` |

---

## 🔄 Как это работает

### 1️⃣ **Обнаружение неактивности**

Во время рассылки бот проверяет каждого пользователя:

```
❌ Ошибка 403 → Пользователь заблокировал бота
❌ Ошибка 400 (chat not found) → Удалил аккаунт
❌ Ошибка 400 (deactivated) → Деактивировал аккаунт
```

### 2️⃣ **Получение данных о подписке**

Бот ищет дату подписки в листе `users` по следующим колонкам (в порядке приоритета):

1. `date_registered`
2. `first_seen`
3. `created_at`
4. `joined_date`

*Если колонка не найдена - поле остается пустым.*

### 3️⃣ **Перенос в архив**

```
1. Добавляет строку в лист "pidarasy":
   - username: из таблицы users
   - tg_id: из таблицы users
   - date on: дата из users (если есть)
   - date off: текущая дата (YYYY-MM-DD)

2. Удаляет строку из листа "users"

3. Логирует в консоль:
   ✅ Перенесен в pidarasy: @username (123456789), 
      подписка: 2026-01-15, отписка: 2026-02-04
```

### 4️⃣ **Отчет в боте**

```
✅ Рассылка завершена!

📊 Статистика:
✉️ Отправлено: 45
❌ Ошибок: 3
📦 Перенесено в архив: 3

Причины:
• Заблокировал бота: 2
• Удалил аккаунт: 1
```

---

## 🧪 Пример работы

### До рассылки:

**Лист `users`:**
| username | telegram_id | date_registered | bot_started |
|----------|-------------|-----------------|-------------|
| @user1 | 111 | 2026-01-10 | TRUE |
| @user2 | 222 | 2026-01-15 | TRUE |
| @user3 | 333 | 2026-01-20 | TRUE |

**Лист `pidarasy`:**
| username | tg_id | date on | date off |
|----------|-------|---------|----------|
| (пусто) | | | |

### Рассылка:

```
✉️ @user1 (111) → ✅ Отправлено
✉️ @user2 (222) → ❌ 403 Forbidden (заблокировал бота)
✉️ @user3 (333) → ✅ Отправлено
```

### После рассылки:

**Лист `users`:**
| username | telegram_id | date_registered | bot_started |
|----------|-------------|-----------------|-------------|
| @user1 | 111 | 2026-01-10 | TRUE |
| @user3 | 333 | 2026-01-20 | TRUE |

**Лист `pidarasy`:**
| username | tg_id | date on | date off |
|----------|-------|---------|----------|
| @user2 | 222 | 2026-01-15 | 2026-02-04 |

---

## 📋 Причины архивации

### Автоматически архивируются:

| Telegram Error | Причина | Описание |
|----------------|---------|----------|
| **403 Forbidden** | Заблокировал бота | Пользователь нажал "Заблокировать" в боте |
| **400 chat not found** | Удалил аккаунт | Пользователь полностью удалил аккаунт Telegram |
| **400 user is deactivated** | Деактивирован | Аккаунт заблокирован Telegram |

### НЕ архивируются (остаются в users):

- **429 Too Many Requests** - временная ошибка (rate limit)
- **400 invalid photo** - ошибка в рассылке (не проблема пользователя)
- **500 Internal Server Error** - ошибка Telegram API

---

## 💡 Применение архива

### Аналитика:

1. **Отток пользователей**
   ```
   Сколько пользователей отписались за месяц?
   → Посчитать строки где date off = январь 2026
   ```

2. **Причины оттока**
   ```
   Почему пользователи уходят?
   → Смотреть на разницу между date on и date off
   → Если уходят быстро (1-2 дня) - проблема с онбордингом
   ```

3. **Реактивация**
   ```
   Можно попробовать связаться с пользователями из архива
   через другие каналы (если есть)
   ```

### Восстановление:

Если пользователь вернется:
1. Бот создаст новую строку в `users`
2. В `pidarasy` останется старая запись (история)
3. Можно отследить повторные подписки

---

## 🔍 Логи

При переносе в архив в консоли Cloudflare появляются записи:

```
✅ Перенесен в pidarasy: @sergeicz (123456789), 
   подписка: 2026-01-15, отписка: 2026-02-04
```

Если возникла ошибка:

```
Failed to move user 123456789 to pidarasy: [описание ошибки]
Failed to delete row 5: [описание ошибки]
```

---

## ⚙️ Настройка листа `pidarasy`

### Создание листа:

1. Откройте вашу Google Таблицу
2. Создайте новый лист с именем **`pidarasy`** (точное название!)
3. Добавьте заголовки в первую строку:

| A | B | C | D |
|---|---|---|---|
| username | tg_id | date on | date off |

### Формат данных:

- **username**: текст, может быть пустым
- **tg_id**: число (Telegram ID)
- **date on**: дата в формате `YYYY-MM-DD` или пусто
- **date off**: дата в формате `YYYY-MM-DD` (заполняется автоматически)

---

## 🚀 Тестирование

### Проверка работы:

1. Создайте тестовую рассылку
2. Заблокируйте бота с тестового аккаунта
3. Запустите рассылку
4. Проверьте лист `pidarasy` - там должна появиться запись

### Ожидаемый результат:

```
📦 Перенесено в архив: 1

Причины:
• Заблокировал бота: 1
```

В листе `pidarasy`:
```
| @testuser | 999888777 | 2026-01-01 | 2026-02-04 |
```

---

## 🔧 Troubleshooting

### Проблема: Пользователи не переносятся в архив

**Решение:**
1. Проверьте что лист называется **точно** `pidarasy`
2. Проверьте заголовки в первой строке (username, tg_id, date on, date off)
3. Посмотрите логи в Cloudflare Workers

### Проблема: date on всегда пустой

**Решение:**
1. Добавьте в лист `users` колонку `date_registered`
2. Заполните датами подписки пользователей
3. Формат: `YYYY-MM-DD` (например: `2026-02-04`)

### Проблема: Удаляются не все пользователи

**Решение:**
- Это нормально! Удаляются только те, кто:
  - Заблокировал бота (403)
  - Удалил аккаунт (400 chat not found)
  - Деактивирован (400 deactivated)
- Другие ошибки НЕ приводят к удалению

---

## 📈 Рекомендации

1. **Регулярно проверяйте архив**
   - Анализируйте причины оттока
   - Смотрите на временные паттерны

2. **Храните исторические данные**
   - Не удаляйте старые записи
   - Используйте для аналитики

3. **Отслеживайте реактивации**
   - Сравнивайте tg_id между users и pidarasy
   - Считайте повторные подписки

4. **Улучшайте онбординг**
   - Если пользователи уходят быстро (date off - date on < 3 дня)
   - Значит нужно улучшить первое впечатление

---

## 📝 Итого

✅ **Автоматический перенос** неактивных в архив  
✅ **Сохранение истории** с датами подписки/отписки  
✅ **Детальная аналитика** причин оттока  
✅ **Чистая база данных** - только активные пользователи  
✅ **Возможность реактивации** - история сохранена  

**Система работает полностью автоматически во время каждой рассылки!** 🚀
