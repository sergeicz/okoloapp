# 🧪 Инструкция по тестированию оптимизаций

## ✅ Что нужно протестировать

### 1. Рассылки (критично!)

**Создайте тестовую рассылку** для проверки батчинга:

1. Откройте бот → `/start`
2. Админ-панель → Новая рассылка
3. Пройдите все шаги (название, партнер, заголовок, текст)
4. Отправьте рассылку

**Что проверить**:
- ✅ Появляются сообщения прогресса: "Прогресс: 20/50", "Прогресс: 40/50"
- ✅ Рассылка проходит **быстро** (если 100 пользователей → ~5-10 секунд)
- ✅ Данные **сохраняются** в лист `broadcasts` в Google Sheets
- ✅ Колонка `partner` заполнена корректно

**Проверка логов** (если есть доступ):
```bash
npx wrangler tail
```
Должны видеть:
- `[getAccessToken] ✅ New token cached` - только первый раз
- `[BROADCAST] ✅ Statistics saved` - после рассылки
- Прогресс батчей

---

### 2. Кабинет партнёра

1. В Google Sheets → лист `partners`
2. В столбце `predstavitel` укажите свой username (например: `ebashordie`)
3. Сохраните
4. В боте → `/start`
5. Должна появиться кнопка **"📊 Кабинет партнёра"**

**Что проверить**:
- ✅ Кнопка появляется
- ✅ Открывается кабинет с вашим партнером
- ✅ Работают отчеты (неделя/месяц)
- ✅ Работает статистика рассылок (если были рассылки по вашему партнеру)

---

### 3. Frontend (Mini App)

1. Откройте Mini App через бот
2. **Проверьте скорость загрузки** - должно быть быстро
3. **Кликните на партнерскую ссылку** → промокод должен прийти в бот
4. **Проверьте, что данные появились** в листе `clicks`

**Тест устойчивости к сбоям**:
- Временно отключите интернет
- Попробуйте открыть Mini App
- Включите интернет
- Страница должна **автоматически загрузиться** (retry logic)

---

### 4. Админ-панель

**Статистика пользователей**:
1. Админ-панель → Пользователи → Активные по активности
2. Должен работать быстро (кэширование)

**Статистика рассылок**:
1. Админ-панель → Статистика рассылок
2. Должны видеть все рассылки с корректными данными

**Отчеты по партнерам**:
1. Админ-панель → Отчеты по партнерам
2. Выберите партнера и период
3. Отчет должен сформироваться быстро

---

## 🐛 Что делать если что-то не работает

### Проблема: Рассылка не сохраняется в broadcasts

**Проверьте**:
1. В Google Sheets есть лист `broadcasts`
2. В листе есть все колонки в правильном порядке (см. `OPTIMIZATION_REPORT.md`)
3. Service Account имеет права на запись

**Исправление**:
- Проверьте логи: `npx wrangler tail`
- Должно быть: `[BROADCAST] ✅ Statistics saved`
- Если ошибка API → проверьте права доступа

### Проблема: Кнопка "Кабинет партнёра" не появляется

**Проверьте**:
1. Username указан **правильно** (без опечаток)
2. Можно указывать с @ или без: `ebashordie` или `@ebashordie`
3. После изменения в Google Sheets подождите **5 минут** (кэш обновится)
4. Или перезапустите worker: `npx wrangler deploy`

### Проблема: Рассылки медленные

**Проверьте логи**:
```bash
npx wrangler tail
```

Должны видеть батчинг:
- `Прогресс: 20/100`
- `Прогресс: 40/100`
- и т.д.

Если батчинга нет → worker не обновился, задеплойте заново:
```bash
cd worker
npx wrangler deploy
```

---

## 📊 Как проверить, что оптимизации работают

### Кэширование токена

**Первый запрос**:
```
[getAccessToken] ✅ New token cached
```

**Следующие запросы** (в течение 55 минут):
```
(нет сообщений о токене - используется из кэша)
```

### Кэширование справочников

**Первая проверка прав**:
```
Admin check for ebashordie (1093761679): true
```

**Следующие проверки** (в течение 5 минут):
```
(быстрее, используется кэш)
```

### Батчинг рассылок

**В логах должно быть**:
```
📊 Прогресс: 20/100 (успешно: 20, ошибок: 0)
📊 Прогресс: 40/100 (успешно: 39, ошибок: 1)
...
```

**Время выполнения**: ~50 секунд на 1000 пользователей

---

## ✅ Чеклист финального тестирования

- [ ] Рассылка работает и сохраняется в broadcasts
- [ ] Кабинет партнёра открывается
- [ ] Отчеты формируются быстро
- [ ] Frontend загружается быстро
- [ ] Промокоды отправляются
- [ ] Клики трекаются в листе clicks
- [ ] Логи не показывают критичных ошибок
- [ ] Админ-панель работает быстро

---

## 🚀 Производительность

**Ожидаемые показатели**:
- Рассылка на 100 пользователей: ~5-10 секунд
- Рассылка на 1000 пользователей: ~50-60 секунд
- Открытие Mini App: ~1-2 секунды
- Проверка прав админа: <10ms
- Формирование отчета: ~1-2 секунды

Если показатели хуже → проверьте логи и обратитесь к `OPTIMIZATION_REPORT.md`
