# 🎯 Как использовать локальную версию

## ✅ Что сейчас работает:

### 1. **Backend API**
- 🟢 Запущен на: `http://127.0.0.1:8787`
- 📝 **Режим**: TEST MODE с редактируемыми данными
- 🔄 **Авто-перезагрузка**: при изменении файлов

### 2. **Frontend**
- 🟢 Запущен на: `http://localhost:8000`
- 🌐 Откройте в браузере
- ✅ Подключен к локальному API

---

## 📝 Как редактировать данные

### Изменить партнерские ссылки:

1. Откройте файл: `worker/mock-data.js`
2. Найдите раздел `partners:`
3. Добавьте/измените ссылки:

```javascript
partners: [
  { title: 'Google', url: 'https://google.com', category: 'Поисковики' },
  // Добавьте свою ссылку:
  { title: 'Мой сайт', url: 'https://mysite.com', category: 'Категория' },
],
```

4. **Сохраните файл** - Worker автоматически перезагрузится
5. **Обновите браузер** (F5) - увидите новые ссылки!

### Добавить себя в админы:

1. Откройте файл: `worker/mock-data.js`
2. Найдите раздел `admins:`
3. Добавьте свой Telegram username **БЕЗ @**:

```javascript
admins: [
  'testuser',
  'ваш_username',  // ← Замените на ваш username
],
```

4. Сохраните файл
5. Обновите браузер - кнопка "Админка" должна появиться!

---

## 🧪 Что можно тестировать:

### ✅ Уже работает:
1. **Отображение ссылок** - видны все ссылки из `mock-data.js`
2. **Клики регистрируются** - смотрите в консоли worker
3. **Админ-панель** - если добавили себя в `admins`
4. **Статистика** - количество пользователей
5. **Тестовая рассылка** - форма отправляется (без реальной отправки)

### ⚠️ Ограничения локального режима:
- Telegram WebApp API работает частично (нет полного доступа к данным пользователя)
- Реальная отправка push-уведомлений не работает
- Данные не сохраняются в Google Sheets

### ✅ Для полного теста:
Используйте **деплой на Cloudflare** - там всё работает с реальными данными!

---

## 🔍 Отладка

### Консоль Worker (логи):

Откройте терминал где запущен `npm run dev` и смотрите логи:

```
👤 Регистрация пользователя: {...}
✅ Новый пользователь: username (123456)
👆 Клик зарегистрирован: 123456 -> https://google.com
🔐 Проверка админа @username: ✅
📢 Push-уведомление: ...
```

### Консоль браузера (F12):

Откройте DevTools → Console для просмотра:
- Ошибок JavaScript
- Network запросов к API
- Ошибок загрузки

---

## 🔄 Горячая перезагрузка

### Worker:
- ✅ **Автоматически** перезагружается при изменении `index.js` или `mock-data.js`
- Смотрите в терминале сообщение "✨ Built in Xms"

### Frontend:
- Просто **обновите страницу** (F5 или Ctrl+R)

---

## 📊 Структура данных

### `mock-data.js` содержит:

```javascript
{
  partners: [...]   // Партнерские ссылки
  users: [...]      // Пользователи (добавляются автоматически)
  admins: [...]     // Username администраторов
  clicks: []        // Клики (добавляются автоматически)
}
```

### Как это работает:

1. При открытии приложения:
   - Загружаются `partners` из mock-data.js
   - Пользователь добавляется в `users`

2. При клике на ссылку:
   - Регистрируется в `clicks`
   - Виден в логах worker

3. При проверке админа:
   - Сравнивается username с списком `admins`
   - Если совпадение - показывается админка

---

## 🎨 Примеры изменений

### Пример 1: Добавить новую категорию ссылок

```javascript
partners: [
  // Существующие...
  { title: 'Netflix', url: 'https://netflix.com', category: 'Развлечения' },
  { title: 'Spotify', url: 'https://spotify.com', category: 'Развлечения' },
],
```

### Пример 2: Добавить несколько админов

```javascript
admins: [
  'admin1',
  'admin2',
  'your_username',
],
```

### Пример 3: Добавить тестового пользователя

```javascript
users: [
  { telegram_id: '123456789', username: 'testuser', first_name: 'Test', subscribed: true },
  { telegram_id: '987654321', username: 'demo', first_name: 'Demo', subscribed: true },
],
```

---

## 🚀 Следующие шаги

### Вариант 1: Продолжить локальное тестирование
1. Редактируйте `mock-data.js`
2. Тестируйте функциональность
3. Проверяйте UI/UX

### Вариант 2: Деплой на продакшен
Когда всё готово:

```bash
# Остановите локальный worker (Ctrl+C)

# Верните продакшн версию
cd worker
ren index.js index.test.bak
ren index.prod.js index.js

# Деплой на Cloudflare
npm run deploy

# Обновите API_URL в frontend/index.html
# Деплой на GitHub Pages
```

---

## 💡 Советы

1. **Регулярно сохраняйте** `mock-data.js`
2. **Смотрите логи** в терминале worker
3. **Используйте DevTools** (F12) для отладки
4. **Тестируйте разные сценарии** (клики, админка, рассылка)
5. **Добавьте реалистичные данные** для лучшего теста

---

## ❓ Частые вопросы

**Q: Почему кнопка "Админка" не появляется?**
A: Проверьте что ваш username добавлен в `admins` в `mock-data.js` БЕЗ символа @

**Q: Данные не сохраняются после перезапуска?**
A: Это нормально для тестового режима. Для сохранения данных используйте деплой с Google Sheets.

**Q: Как увидеть реальные данные из Google Sheets?**
A: Деплойте на Cloudflare - там worker подключится к реальной таблице.

**Q: Worker не перезагружается?**
A: Проверьте что нет синтаксических ошибок в `mock-data.js`. Смотрите логи в терминале.

---

**🎉 Готово! Теперь у вас полностью рабочая локальная версия для тестирования!**

**📝 Откройте `worker/mock-data.js` и добавьте свои данные!**
