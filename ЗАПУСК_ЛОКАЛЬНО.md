# 💻 ЗАПУСК БОТА ЛОКАЛЬНО - ЗА 3 МИНУТЫ

## ✅ САМЫЙ ПРОСТОЙ СПОСОБ!

Бот будет работать на вашем компьютере. Не нужны облачные сервисы!

---

## ШАГ 1: Проверьте Python

Откройте PowerShell или CMD и проверьте:

```bash
python --version
```

Если Python не установлен:
- Скачайте с https://www.python.org/downloads/
- Установите (галочка "Add to PATH"!)
- Перезапустите терминал

---

## ШАГ 2: Установите зависимости

```bash
cd "C:\Users\workn\Documents\sites\telegram-miniapp\bot"
pip install -r requirements.txt
```

Подождите ~1 минуту пока установятся библиотеки.

---

## ШАГ 3: Убедитесь что все файлы на месте

В папке `bot/` должны быть:
- ✅ bot.py
- ✅ requirements.txt
- ✅ ../credentials.json (на уровень выше)
- ✅ ../bot_token.txt
- ✅ ../sheet_id.txt

---

## ШАГ 4: Создайте WEBAPP_URL (если нет)

Создайте файл `webapp_url.txt` в папке `bot/`:

```
https://sergeicz.github.io/tg_miniapps_links/frontend/
```

Или бот возьмет из переменной окружения.

---

## ШАГ 5: ЗАПУСТИТЕ!

```bash
python bot.py
```

### ✅ Должно появиться:

```
INFO:Bot started successfully!
INFO:Polling started...
```

**БОТ РАБОТАЕТ!** 🎉

---

## ШАГ 6: Проверьте в Telegram

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Должно открыться Mini App!

---

## 📊 ЛОГИ

Все действия бота записываются в файл `bot.log` в папке bot/.

---

## ⚠️ ВАЖНО:

### Чтобы бот работал:
- ✅ Окно терминала должно быть открыто
- ✅ ПК должен быть включен
- ✅ Интернет должен работать

### Чтобы остановить бота:
- Нажмите `Ctrl+C` в терминале

### Чтобы бот работал в фоне:
Windows: используйте `pythonw.exe` вместо `python`

---

## 🔄 АВТОЗАПУСК ПРИ СТАРТЕ WINDOWS (ОПЦИОНАЛЬНО)

### Вариант 1: Через Task Scheduler

1. Win+R → `taskschd.msc`
2. Create Basic Task
3. Trigger: When I log on
4. Action: Start a program
5. Program: `python`
6. Arguments: `C:\Users\workn\Documents\sites\telegram-miniapp\bot\bot.py`
7. Start in: `C:\Users\workn\Documents\sites\telegram-miniapp\bot`

### Вариант 2: Через bat-файл в автозагрузке

Создайте `start_bot.bat`:
```bat
@echo off
cd "C:\Users\workn\Documents\sites\telegram-miniapp\bot"
python bot.py
pause
```

Поместите в:
```
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
```

---

## 🎊 ГОТОВО!

Теперь бот:
- ✅ Отвечает на команду /start
- ✅ Открывает Mini App
- ✅ Отправляет push-рассылки из админки
- ✅ Логирует все действия

**Не закрывайте окно терминала пока нужен бот!**

---

## ❓ ПРОБЛЕМЫ?

### "No module named 'aiogram'"
```bash
pip install aiogram gspread google-auth
```

### "File not found: credentials.json"
Проверьте что файл `credentials.json` в корне проекта.

### Бот не отвечает
- Проверьте токен в `bot_token.txt`
- Проверьте что бот не заблокирован в @BotFather
- Проверьте логи в `bot.log`

---

**Запускайте прямо сейчас!** 🚀
