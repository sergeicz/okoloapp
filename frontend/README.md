# Telegram Mini App Frontend

## Описание
Веб-интерфейс Telegram Mini App для просмотра партнерских ссылок и администрирования.

## Деплой на GitHub Pages

### 1. Создайте репозиторий на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/yourusername/telegram-miniapp.git
git push -u origin main
```

### 2. Настройте GitHub Pages
1. Зайдите в настройки репозитория (Settings)
2. Перейдите в раздел "Pages"
3. Source: выберите "Deploy from a branch"
4. Branch: выберите `main` и папку `/frontend` (или root, если файлы в корне)
5. Нажмите "Save"

### 3. Настройте конфигурацию
Откройте `index.html` и измените URL API:
```javascript
const CONFIG = {
  API_URL: 'https://your-worker.your-domain.workers.dev',
};
```

Замените на URL вашего Cloudflare Worker.

### 4. Обновите изменения
```bash
git add index.html
git commit -m "Update API URL"
git push
```

GitHub Pages автоматически обновится через 1-2 минуты.

### 5. Получите URL вашего приложения
URL будет:
```
https://yourusername.github.io/telegram-miniapp/
```

или (если используете custom domain):
```
https://your-domain.com
```

## Настройка Telegram Mini App

### 1. Откройте BotFather
Отправьте команду `/mybots` и выберите вашего бота

### 2. Настройте Web App
1. Bot Settings → Menu Button → Configure menu button
2. Menu Button URL: вставьте URL вашего GitHub Pages
3. Menu Button Text: `🚀 Открыть приложение`

### 3. Добавьте кнопку в inline режим (опционально)
1. Bot Settings → Inline Mode → Turn on
2. Добавьте Web App кнопку в inline keyboard

## Структура проекта
```
frontend/
├── index.html          # Главный файл приложения
└── README.md          # Эта инструкция
```

## Особенности

### Telegram WebApp API
- Автоматическое расширение на весь экран
- Адаптация под тему Telegram
- Haptic Feedback при кликах
- Встроенные алерты через Telegram

### Адаптивный дизайн
- Работает на всех размерах экранов
- Glassmorphism UI с тёмной темой
- Плавные анимации и переходы

### Администрирование
- Панель администратора (доступна только админам)
- Статистика пользователей
- Рассылка push-уведомлений
- Просмотр списка подписчиков

## Кастомизация

### Изменение темы
Измените CSS переменные в `<style>`:
```css
:root {
  --bg-color: #121212;
  --primary-color: #4f9dff;
  --accent-color: #28a745;
  --text-color: #f0f0f0;
}
```

### Изменение аватара
Замените URL изображения:
```html
<img src="https://i.pravatar.cc/150?img=12" alt="Аватар">
```

## Тестирование

### Локальное тестирование
1. Используйте [Telegram Web](https://web.telegram.org)
2. Откройте Developer Tools (F12)
3. Эмулируйте мобильное устройство
4. Откройте URL приложения

### Тестирование на устройстве
1. Отправьте URL себе в Telegram
2. Откройте в браузере
3. Проверьте все функции

## Возможные проблемы

### API не отвечает
- Проверьте, что Cloudflare Worker задеплоен
- Проверьте правильность API_URL в коде
- Откройте Developer Tools и проверьте Console на ошибки

### Кнопка "Админка" не появляется
- Проверьте, что ваш username добавлен в лист "admins" в Google таблице
- Проверьте Console на ошибки API

### Ссылки не открываются
- Проверьте, что в таблице "partners" правильные URL (с http:// или https://)
- Проверьте CORS настройки в Worker

## Custom Domain (опционально)

Если хотите использовать свой домен:

1. Добавьте файл `CNAME` в корень проекта:
```
your-domain.com
```

2. Настройте DNS записи у вашего провайдера:
```
Type: CNAME
Host: www (или @)
Value: yourusername.github.io
```

3. В настройках GitHub Pages укажите custom domain

## SEO и метатеги

Добавьте в `<head>` для улучшения SEO:
```html
<meta name="description" content="Описание вашего приложения">
<meta property="og:title" content="Название">
<meta property="og:description" content="Описание">
<meta property="og:image" content="https://url-to-preview-image.jpg">
```

## Безопасность
- Все чувствительные данные хранятся на backend (Worker)
- Frontend не содержит токенов или ключей
- Все API запросы идут через HTTPS
