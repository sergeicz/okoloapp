# 🖥️ Локальный запуск проекта для тестирования

## 📋 Предварительные требования

Убедитесь, что у вас установлено:
- ✅ Python 3.8+ ([python.org](https://www.python.org/downloads/))
- ✅ Node.js 16+ ([nodejs.org](https://nodejs.org/))
- ✅ Git ([git-scm.com](https://git-scm.com/))

Проверка версий:
```bash
python --version
node --version
npm --version
```

---

## ⚙️ Шаг 1: Подготовка файлов конфигурации (2 минуты)

### 1.1 Создайте файл с токеном бота

В корне проекта создайте файл `bot_token.txt`:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```
(замените на ваш токен от @BotFather)

### 1.2 Создайте файл с ID таблицы

В корне проекта создайте файл `sheet_id.txt`:
```
1AbCdEfGhIjKlMnOpQrStUvWxYz123456789
```
(замените на ID вашей Google таблицы)

### 1.3 Создайте файл credentials.json

В корне проекта создайте файл `credentials.json` и вставьте JSON от Google Service Account:
```json
{
  "type": "service_account",
  "project_id": "your-project",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "your-service-account@your-project.iam.gserviceaccount.com",
  ...
}
```

**⚠️ ВАЖНО:** Эти файлы уже в `.gitignore`, они не попадут в Git!

---

## 🚀 Шаг 2: Запуск Backend API локально (3 минуты)

### 2.1 Установите зависимости

Откройте терминал в папке проекта:

```bash
# Перейдите в папку worker
cd worker

# Установите зависимости
npm install
```

### 2.2 Создайте файл .dev.vars

В папке `worker/` создайте файл `.dev.vars`:

```bash
# worker/.dev.vars
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz123456789
CREDENTIALS_JSON={"type":"service_account","project_id":"..."}
```

**Важно:** 
- CREDENTIALS_JSON должен быть в одну строку (минифицированный JSON)
- Используйте [jsonformatter.org/json-minify](https://jsonformatter.org/json-minify)

### 2.3 Запустите Worker локально

```bash
npm run dev
```

Вы увидите:
```
⎔ Starting local server...
[wrangler:inf] Ready on http://localhost:8787
```

### 2.4 Проверьте API

Откройте в браузере или через curl:
```
http://localhost:8787/api/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"...","version":"1.0.0"}
```

Другие эндпоинты для проверки:
```
http://localhost:8787/api/partners
```

✅ **Worker работает локально!**

**Оставьте терминал открытым** - Worker должен продолжать работать.

---

## 🎨 Шаг 3: Настройка Frontend (2 минуты)

### 3.1 Обновите API_URL

Откройте файл `frontend/index.html` и измените:

```javascript
const CONFIG = {
  API_URL: 'http://localhost:8787',  // Локальный Worker
};
```

### 3.2 Запустите Frontend

**Вариант 1 - Простой (Python):**

Откройте **НОВЫЙ терминал** в папке `frontend/`:

```bash
cd frontend
python -m http.server 8000
```

Frontend будет доступен на: `http://localhost:8000`

**Вариант 2 - Node.js (если установлен http-server):**

```bash
cd frontend
npx http-server -p 8000
```

**Вариант 3 - Прямое открытие файла:**

Просто откройте `frontend/index.html` в браузере (но Telegram API не будет работать полноценно).

### 3.3 Откройте в браузере

```
http://localhost:8000
```

**⚠️ Важно:** Telegram WebApp API не будет полноценно работать вне Telegram, но вы сможете протестировать:
- Загрузку партнерских ссылок
- API запросы
- Админ-панель (частично)

Для полного тестирования нужно использовать через Telegram (см. Шаг 5).

✅ **Frontend работает локально!**

---

## 🤖 Шаг 4: Запуск Telegram бота локально (3 минуты)

### 4.1 Установите зависимости

Откройте **НОВЫЙ терминал** в корне проекта:

```bash
cd bot

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 4.2 Настройте переменные окружения (опционально)

Если хотите использовать переменные окружения вместо файлов, создайте файл `.env` в папке `bot/`:

```bash
# bot/.env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz123456789
WEBAPP_URL=http://localhost:8000
```

И установите python-dotenv:
```bash
pip install python-dotenv
```

### 4.3 Запустите бота

```bash
python bot.py
```

Вы увидите:
```
INFO - Бот успешно инициализирован
INFO - Бот запущен и готов к работе
```

### 4.4 Протестируйте бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить!

✅ **Бот работает локально!**

---

## 🧪 Шаг 5: Полное тестирование через Telegram

Для полноценного тестирования Mini App нужно сделать локальный сервер доступным из интернета.

### Вариант 1: Ngrok (рекомендуется для тестирования)

#### 5.1 Установите Ngrok

1. Скачайте с [ngrok.com](https://ngrok.com/download)
2. Распакуйте и зарегистрируйтесь (бесплатно)
3. Авторизуйтесь: `ngrok authtoken YOUR_TOKEN`

#### 5.2 Запустите туннель для Frontend

Откройте **НОВЫЙ терминал**:

```bash
ngrok http 8000
```

Вы получите публичный URL:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:8000
```

#### 5.3 Настройте бота с Ngrok URL

**Временно** измените `WEBAPP_URL`:

В файле `bot.py` или переменной окружения укажите:
```
WEBAPP_URL = https://abc123.ngrok.io
```

Перезапустите бота.

#### 5.4 Настройте Menu Button

Откройте @BotFather:
```
/mybots → Ваш бот → Bot Settings → Menu Button
→ Configure menu button
→ Вставьте: https://abc123.ngrok.io
→ Текст: 🚀 Тестовая версия
```

#### 5.5 Протестируйте!

1. Откройте бота в Telegram
2. `/start`
3. Нажмите кнопку Mini App
4. Приложение откроется из локального сервера!

**Теперь можно тестировать:**
- ✅ Telegram WebApp API работает полноценно
- ✅ Все кнопки и функции
- ✅ Админ-панель
- ✅ Рассылки

### Вариант 2: Localtunnel (альтернатива)

```bash
npx localtunnel --port 8000
```

### Вариант 3: Cloudflare Tunnel (более сложно)

Для продвинутых пользователей - создает постоянный туннель.

---

## 📊 Проверка всех компонентов

### Проверочный список:

**Backend (Worker):**
- [ ] `http://localhost:8787/api/health` возвращает OK
- [ ] `http://localhost:8787/api/partners` возвращает данные из Google Sheets
- [ ] В терминале нет ошибок

**Frontend:**
- [ ] `http://localhost:8000` открывается
- [ ] Отображаются партнерские ссылки
- [ ] В консоли браузера (F12) нет ошибок CORS
- [ ] Запросы к API успешны (см. Network tab)

**Bot:**
- [ ] Бот запущен без ошибок
- [ ] Отвечает на `/start`
- [ ] Логи пишутся в консоль

**Интеграция (через Ngrok):**
- [ ] Mini App открывается из Telegram
- [ ] Данные загружаются
- [ ] Клики регистрируются
- [ ] Админка работает (если вы в списке admins)

---

## 🛠️ Структура запущенных процессов

У вас должно быть **3-4 открытых терминала**:

```
Терминал 1: worker/
└─> npm run dev           (http://localhost:8787)

Терминал 2: frontend/
└─> python -m http.server 8000    (http://localhost:8000)

Терминал 3: bot/
└─> python bot.py         (Telegram Bot работает)

Терминал 4 (опционально):
└─> ngrok http 8000       (Публичный доступ)
```

---

## 🔍 Тестирование API вручную

### Тест 1: Health Check
```bash
curl http://localhost:8787/api/health
```

### Тест 2: Получить партнеров
```bash
curl http://localhost:8787/api/partners
```

### Тест 3: Зарегистрировать пользователя
```bash
curl -X POST http://localhost:8787/api/user \
  -H "Content-Type: application/json" \
  -d '{"id": 123456789, "username": "testuser", "first_name": "Test"}'
```

### Тест 4: Проверить админа
```bash
curl -X POST http://localhost:8787/api/me \
  -H "Content-Type: application/json" \
  -d '{"username": "your_username"}'
```

### Тест 5: Зарегистрировать клик
```bash
curl -X POST http://localhost:8787/api/click \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789, "url": "https://google.com"}'
```

---

## 📝 Просмотр логов

### Worker логи
Смотрите в терминале где запущен `npm run dev`

### Frontend логи
Откройте DevTools (F12) → Console

### Bot логи
- В терминале где запущен `python bot.py`
- В файле `bot/bot.log`

---

## 🐛 Решение проблем

### Worker не запускается

```bash
# Очистите кэш и переустановите зависимости
cd worker
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Frontend CORS ошибки

Убедитесь что:
1. Worker запущен на `localhost:8787`
2. В `index.html` указан `API_URL: 'http://localhost:8787'`
3. Worker правильно возвращает CORS заголовки

### Bot не находит файлы

Убедитесь что файлы существуют:
```bash
# Проверьте наличие файлов
ls bot_token.txt
ls sheet_id.txt
ls credentials.json
```

### Google Sheets ошибки

1. Проверьте что Service Account email добавлен в редакторы таблицы
2. Проверьте что Google Sheets API включен
3. Проверьте формат credentials.json

### Ngrok истек

Бесплатный Ngrok дает временные URL. При перезапуске URL меняется:
1. Запустите `ngrok http 8000` снова
2. Обновите URL в @BotFather

---

## 🎯 Что тестировать

### Базовые функции:
1. **Загрузка ссылок** - открываются ли партнерские ссылки
2. **Регистрация** - появляется ли ваш ID в Google Sheets
3. **Клики** - регистрируются ли клики в листе `clicks`
4. **Админка** - видна ли кнопка если вы в `admins`

### Продвинутые функции:
5. **Статистика** - правильно ли считается количество пользователей
6. **Рассылка** - отправляются ли пуш-уведомления
7. **Обработка ошибок** - что происходит при ошибках API
8. **Rate limiting** - как ведет себя при множественных запросах

---

## 🔄 Горячая перезагрузка (Hot Reload)

### Worker
`wrangler dev` автоматически перезагружается при изменении `index.js`

### Frontend
Просто обновите страницу в браузере (F5 или Ctrl+R)

### Bot
Остановите (Ctrl+C) и запустите снова:
```bash
python bot.py
```

---

## ⏹️ Остановка всех сервисов

Нажмите `Ctrl+C` в каждом терминале:

1. Остановите Worker (Ctrl+C в терминале с npm run dev)
2. Остановите Frontend сервер (Ctrl+C)
3. Остановите Bot (Ctrl+C)
4. Остановите Ngrok (Ctrl+C, если использовали)

---

## 📦 Работа с виртуальным окружением Python

### Активация:
```bash
# Windows
bot\venv\Scripts\activate

# macOS/Linux
source bot/venv/bin/activate
```

### Деактивация:
```bash
deactivate
```

### Удаление (если нужно пересоздать):
```bash
# Windows
rmdir /s bot\venv

# macOS/Linux
rm -rf bot/venv
```

---

## 🎓 Полезные команды

### Проверить что порт занят (Windows):
```bash
netstat -ano | findstr :8787
netstat -ano | findstr :8000
```

### Убить процесс на порту (Windows):
```bash
taskkill /PID <PID> /F
```

### Проверить что порт занят (macOS/Linux):
```bash
lsof -i :8787
lsof -i :8000
```

### Убить процесс на порту (macOS/Linux):
```bash
kill -9 <PID>
```

---

## ✅ Когда локальное тестирование завершено

После успешного тестирования:

1. **Верните API_URL в frontend**:
   ```javascript
   const CONFIG = {
     API_URL: 'https://your-worker.workers.dev',  // Продакшен URL
   };
   ```

2. **Закоммитьте изменения**:
   ```bash
   git add .
   git commit -m "Tested locally, ready for production"
   ```

3. **Деплойте на продакшен** согласно `ИНСТРУКЦИЯ_ДЕПЛОЙ.md`

---

## 🎉 Готово!

Теперь вы можете:
- ✅ Разрабатывать и тестировать локально
- ✅ Видеть изменения мгновенно
- ✅ Дебажить код эффективно
- ✅ Тестировать без деплоя

**Удачи с разработкой! 🚀**
